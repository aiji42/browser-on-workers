node_modules/
crate/target/
.wrangler/
*.png
!docs/*.png
# 3 つのブラウザの比較。ページから参照しているので、無いとデモが壊れる
!public/shots/*.png
# 動的 Worker に渡すための engine 本体。pkg からのコピー (npm run build で作る)
public/engine.wasm

# 切り分けに使った第三者のページの HTML (952KB)。テストは無ければ skip する
crate/fixtures/
