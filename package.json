{
  "name": "browser-on-workers",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "URL を渡すとスクリーンショットが返る、Cloudflare Workers の上だけで動く小さなブラウザ",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "fonts": "node scripts/build-fonts.mjs",
    "dev": "wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "shot": "node test/shot.mjs",
    "build:wasm": "cd crate && cargo build --release --target wasm32-unknown-unknown && wasm-bindgen --target web --out-dir pkg --no-typescript target/wasm32-unknown-unknown/release/kitesurf_clone.wasm",
    "build": "npm run fonts && npm run build:wasm",
    "shots": "node scripts/build-shots.mjs"
  },
  "devDependencies": {
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "@fontsource/noto-sans-jp": "^5.3.0",
    "subset-font": "^2.7.0",
    "wrangler": "^4.128.0"
  }
}
